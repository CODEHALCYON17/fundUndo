{
  "name": "ieee-funding-digest",
  "version": "0.1.0",
  "private": true,
  "description": "Scrapes curated IEEE funding/grant/scholarship pages, extracts structured opportunities (amount, deadline, summary) via Claude, and emails a digest of new or changed listings.",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "dev:dry": "tsx src/index.ts --dry-run"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "nodemailer": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/nodemailer": "^6.4.16",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
